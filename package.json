{
  "name": "typescript-base",
  "version": "1.0.0",
  "description": "1. Create directory (mkdir <dir>)",
  "main": "index.js",
  "scripts": {
    "build": "tsc *.ts",
    "watch": "tsc --watch --outDir build *.ts",
    "start": "node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.5.4"
  }
}
